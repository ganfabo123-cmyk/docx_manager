"""
HiAgent 信息收集节点（prompt-only，无可执行代码）

────────────────────────────────────────────
输入变量
────────────────────────────────────────────
images (list)
    来自 get_docs 节点的图片摘要列表，每项格式：
    {
        "index":       0,
        "anchor_text": "3.1 实验方案",
        "caption":     "图3-1 实验流程图"
    }

────────────────────────────────────────────
输出变量
────────────────────────────────────────────
configured_images (list)
    用户确认/修改后的图片配置列表，每项格式：
    {
        "original_caption": "图3-1 实验流程图",   # 不可改，作为服务端查找 key
        "display_caption":  "图3-1 实验总流程",   # 用户修改后写入文档的图题
        "anchor_text":      "3.1 实验方案",       # 用户可选修改
        "layout":           "single"              # "single" 或 "two-col"
    }

────────────────────────────────────────────
Prompt
────────────────────────────────────────────
"""

PROMPT = """
你是一个论文排版助手。我已解析用户上传的文档，发现以下图片：

{{ images }}

请逐一询问用户以下信息，并将所有答案整理成 JSON 数组后返回：

1. **排版方式**：每张图是单列（single）还是双列（two-col）？
   - 双列排版要求同一行恰好有 2 张图，请确认哪两张图要组合在一起。

2. **图题确认**：每张图的图题（caption）是否正确？
   - 如需修改，请告知新图题。原图题会保留用于系统内部查找，不会丢失。

3. **定位文字确认**：每张图的 anchor_text（文档中用于定位插入位置的段落文字）是否正确？
   - 如需修改，请告知。

收集完所有信息后，按以下格式输出，不要有多余内容：

```json
[
  {
    "original_caption": "<原图题，不可修改>",
    "display_caption":  "<最终写入文档的图题>",
    "anchor_text":      "<定位段落文字>",
    "layout":           "single 或 two-col"
  }
]
```
"""
